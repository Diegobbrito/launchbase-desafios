

{% extends "layout.njk" %}

{% block content %}

<div class="card">
        <div class="header-chef-create">
            <h1>Chef: {{chef.name}}</h1>
            <a href="/admin/chefs/{{chef.id}}/edit" class="button">Editar chef</a>
        </div>
        <div class="chef-image">
            <img src="/images/{{chef.image}}" >
        </div>
        <div class="chef-description">
        <h1>{{chef.name}}</h1>
        <span>x receitas</span>
        </div>
</div>
<script src="/public/script.js"></script>
{% endblock content %}